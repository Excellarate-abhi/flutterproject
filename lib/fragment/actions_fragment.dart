import 'package:flutter/material.dart';

class Action_Fragment extends StatelessWidget {
  const Action_Fragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hello Fragment 3"),
    );
  }
}
