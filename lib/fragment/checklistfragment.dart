import 'package:flutter/material.dart';

class Checklist_Fragment extends StatelessWidget {
  const Checklist_Fragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hello Fragment 2"),
    );
  }
}
